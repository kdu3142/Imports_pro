services:
  app:
    build: .
    ports:
      - "3333:3000"
    environment:
      NODE_ENV: production
      PORT: 3000
    user: "${UID:-1000}:${GID:-1000}"
    volumes:
      - ./data:/app/.local
    restart: unless-stopped
