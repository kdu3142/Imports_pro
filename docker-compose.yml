version: "3.9"

services:
  app:
    build: .
    ports:
      - "3333:3000"
    environment:
      NODE_ENV: production
      PORT: 3000
    volumes:
      - ./data:/app/.local
    restart: unless-stopped

volumes:
  projects_data:
    driver: local
